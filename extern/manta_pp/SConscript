#!/usr/bin/python
import sys
import os


# env = BlenderEnvironment(ENV = os.environ) # create an environment
Import('env')

# defs = ''
# cflags = []

# src = env.Glob('*')

# defs = ''
incs = env['BF_PYTHON_INC']
env['BF_PYTHON_INC'],
defs.append('WITH_PYTHON') 
env.BlenderLib(libname='extern_manta', sources=env.Glob('*'), Split(incs), defs) 
